# --- Web application ---------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-dotenv>=1.0
pydantic>=2.6
python-multipart>=0.0.9
pillow>=10.2
aiofiles>=23.2
psutil>=5.9
nvidia-ml-py>=12.535   # provides the pynvml module for GPU/VRAM stats (optional at runtime)

# --- Real Wan 2.2+ generation backend -----------------------------------
# NOTE: install PyTorch with the CUDA build matching your GPU FIRST, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (see README → Real Wan Backend Requirements)
torch>=2.7
diffusers>=0.35
transformers>=4.46
accelerate>=1.0
safetensors>=0.4
sentencepiece>=0.2
ftfy>=6.1
numpy>=1.26
imageio-ffmpeg>=0.4.9
